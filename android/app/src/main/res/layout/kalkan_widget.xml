<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="#12141A">
    <TextView android:text="KALKAN" android:textColor="#8A909D" android:textSize="10sp" />
    <LinearLayout android:orientation="horizontal">
        <TextView android:id="@+id/widget_recovery" android:text="—" android:textColor="#EDEDED" android:textSize="22sp" android:layout_marginEnd="12dp" />
        <TextView android:id="@+id/widget_strain" android:text="—" android:textColor="#EDEDED" android:textSize="22sp" android:layout_marginEnd="12dp" />
        <TextView android:id="@+id/widget_hr" android:text="—" android:textColor="#EDEDED" android:textSize="22sp" />
    </LinearLayout>
</LinearLayout>
